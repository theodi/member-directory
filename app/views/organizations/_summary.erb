<div class='organization organization-summary' vocab="http://schema.org/" typeOf="Organization">

  <div class='organization-logo pull-right'
       style='background-image: url(<%= organization.logo.rectangular.url %>)'>
  </div>
  <div class='organization-content'>
    <div>
      <h2 property="name"><%= link_to organization.name, organization.member %></h2><span class="label <%= organization.member.product_name %>"><%= organization.member.product_name.capitalize %></span>
    </div>

    <% if organization.cached_tagline.present? %>
      <p><strong><%= organization.cached_tagline %></strong></p>
    <% end %>

    <div>
      <i class='fa fa-home'></i> <%= link_to organization.url, organization.url, :property => "url"%></a>
    </div>

    <div class='organization-description clearfix'>
      <p property="description"><%= organization_description(organization) %></p>
      <%= link_to "#{editable?(organization) ? "Edit" : "More"} information &raquo;".html_safe, organization.member, :class=>'btn btn-default pull-right' %>
    </div>
  </div>

</div>
