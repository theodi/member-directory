<% @organizations.each do |organization| %>
  <%= render :partial => 'organizations/summary', :locals => {:organization => organization}%>
<% end %>

<div class="btn-toolbar pull-right">
  <div class="btn-group">
    <%= alternate_button(:json) %>
  </div>
</div>

<div style='clear:right'>
</div>